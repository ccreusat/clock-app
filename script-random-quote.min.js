!function(){"use strict";const quoteButton=document.getElementById("quote-button");function getRandomNumber(max){return Math.floor(Math.random()*max)}async function getRandomQuote(){const quoteURL="https://mindjournal-app.herokuapp.com/",query=()=>"{\n          quotes {\n            text\n            author {\n              name\n            }\n          }\n        }",options={method:"post",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:"{\n          quotes {\n            text\n            author {\n              name\n            }\n          }\n        }"})};await fetch(quoteURL,options).then(response=>response.json()).then(({data:data})=>{let quotes=data.quotes,quotesLength,randomNumber,randomQuote=quotes[getRandomNumber(quotes.length)];const quoteContainer=document.getElementById("quote"),quoteAuthor=document.getElementById("quote-author");quoteContainer.innerHTML=randomQuote.text,quoteAuthor.innerHTML=randomQuote.author.name})}quoteButton.onclick=()=>getRandomQuote()}();